import { Typography } from '@mui/material';
import React from 'react';

const Pay = () => {
    return (
        <div>
            <Typography variant="h3">Payment system is comming soon</Typography>
        </div>
    );
};

export default Pay;