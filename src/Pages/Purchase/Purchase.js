import React from 'react';
import useAuth from '../../Hooks/useAuth'
const Purchase = () => {
    const { user } = useAuth();

    return (
        <div>

        </div>
    );
};

export default Purchase;